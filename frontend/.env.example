# Vercel frontend project
INTERNAL_API_URL=https://app-redacao-back.vercel.app/api/v1
NEXT_PUBLIC_SITE_URL=https://app-redacao-five.vercel.app
# Mesmo valor de JWT_SECRET_KEY do backend — usado só server-side (proxy.ts) pra validar a
# assinatura do cookie de sessão antes de liberar rota protegida.
JWT_SECRET_KEY=change-this-secret-before-production
