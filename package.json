{
  "name": "donk-enem-platform",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "docker compose up --build",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "cd backend && python -m uvicorn src.main:app --reload --port 8000",
    "build": "npm --prefix frontend run build && cd backend && python -m compileall src tests alembic",
    "test": "npm --prefix frontend run lint && npm --prefix frontend run typecheck && cd backend && python -m pytest",
    "start:frontend": "npm --prefix frontend run start",
    "start:backend": "cd backend && python -m uvicorn src.main:app --host 0.0.0.0 --port 8000",
    "deploy:frontend": "npm --prefix frontend run build",
    "deploy:backend": "cd backend && python -m compileall src alembic"
  }
}
